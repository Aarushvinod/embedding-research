# ByteEmbed feasibility (Colab A100 / cloud GPU — training)
#
# IMPORTANT (Blackwell / RTX 50-series local use): install a cu128 build, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# Colab / cloud A100 images already ship a compatible torch; the line below is a floor.
torch>=2.7
transformers>=4.44
sentence-transformers>=3.0
datasets>=2.20
accelerate>=0.33
sentencepiece>=0.2   # mt5-small tokenizer (subword iso-compute baseline)
tiktoken>=0.7        # newer transformers' slow->fast tokenizer conversion path requires it
protobuf>=4.25       # ditto (sentencepiece conversion); absent on bare clusters unlike Colab
scipy>=1.11           # STS Spearman; bootstrap stats
scikit-learn>=1.3     # logistic-regression probes (SIB classification)
numpy>=1.26
tqdm>=4.66
matplotlib>=3.8

# Optional: decouples contrastive batch size from VRAM (only needed for the
# contrastive route; the default distillation route does not require it).
# grad-cache>=0.1.0

# SONAR teacher (run_lowresource.py): loaded by default via the fairseq2-FREE HF port
# `cointegrated/SONAR_200_text_encoder` (uses `transformers` above — no extra install, no torch
# change, 1024-d, validated to match official SONAR). The official `sonar-space`/fairseq2 stack is
# OPTIONAL (notebook cell 3) and needs torch pinned to a fairseq2 wheel; `load_teacher()` prefers it
# if present, else the HF port, else LaBSE.
# Optional: FLOPs counting in efficiency.profile_model (degrades to None if absent).
# fvcore>=0.1.5
